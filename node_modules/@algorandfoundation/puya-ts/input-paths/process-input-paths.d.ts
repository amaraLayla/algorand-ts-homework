import type { AlgoFile } from '../options';
export declare const processInputPaths: ({ paths, workingDirectory, outDir, }: {
    paths: string[];
    outDir?: string;
    workingDirectory?: string;
}) => AlgoFile[];
